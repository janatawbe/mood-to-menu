# The client currently has no environment variables of its own.
# API requests go through the relative "/api" path, proxied to the backend by Vite in
# development and by the hosting setup in production — no backend URL needs to be configured here.
#
# Do NOT add GEMINI_API_KEY or any other secret here. Secrets are server-side only (see server/.env.example).

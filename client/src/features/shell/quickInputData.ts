/** The Vibe Check's quick-select chips — structured selections kept separate from the
 * free-text value (see useVibeCheck), not inserted into the textarea as text. */
export const quickInputOptions = [
  "Long day",
  "Need comfort",
  "Too tired to cook",
  "Healthy please",
  "Something light",
] as const;
